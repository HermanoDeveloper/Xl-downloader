
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xadreklabs.statussaver',
  appName: 'XL Status Saver',
  webDir: 'dist'
};

export default config;
